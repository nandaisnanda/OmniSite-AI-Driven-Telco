# Development and CI tooling. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==8.4.1
pytest-cov==6.2.1
ruff==0.12.4
mypy==1.16.1
# requests ships no inline types; without these mypy reports the import as untyped.
types-requests==2.32.4.20250611
pip-audit==2.8.0
